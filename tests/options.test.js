const test=require('node:test');const assert=require('node:assert/strict');const {bullCallSpread,ironCondor}=require('../src/sat/strategies/definitions');const {payoffSummary}=require('../src/sat/strategies/payoff');const {validateChainLiquidity}=require('../src/sat/options/engine');const {chooseExpiry,rankContracts}=require('../src/sat/options/selector');
test('bull call spread has exact two legs and defined risk',()=>{const s=bullCallSpread({expiry:'2026-09-03',longStrike:25000,shortStrike:25200,longPremium:150,shortPremium:70});const p=payoffSummary(s.legs,s.netPremium);assert.equal(s.legs.length,2);assert.equal(p.maxLoss,-80);assert.equal(p.maxProfit,120);assert.deepEqual(p.breakevens,[25080]);});
test('iron condor exposes four legs',()=>{const s=ironCondor({expiry:'2026-09-03',putLong:24500,putShort:24700,callShort:25300,callLong:25500,putLongPremium:30,putShortPremium:70,callShortPremium:65,callLongPremium:25});assert.equal(s.legs.length,4);assert.equal(s.legs.filter(l=>l.side==='SELL').length,2);});
test('liquidity gate rejects wide spread',()=>{assert.equal(validateChainLiquidity({bid:10,ask:11,volume:100}).ok,false);});
test('expiry selection chooses nearest future expiry',()=>{assert.equal(chooseExpiry(['2026-08-27','2026-09-03','2026-09-10'],new Date('2026-08-29')),'2026-09-03');});
test('contract ranking prefers liquid tight-spread quotes',()=>{const r=rankContracts([{strike:25000,bid:100,ask:101,volume:1000},{strike:25100,bid:90,ask:100,volume:5000}]);assert.equal(r[0].strike,25000);});
