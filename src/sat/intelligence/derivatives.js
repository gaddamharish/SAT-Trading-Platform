function optionChainMetrics(chain=[]){const rows=Array.isArray(chain)?chain:[];let callOI=0,putOI=0,callChange=0,putChange=0;for(const r of rows){callOI+=Number(r.callOI)||0;putOI+=Number(r.putOI)||0;callChange+=Number(r.callOIChange)||0;putChange+=Number(r.putOIChange)||0;}return {callOI,putOI,pcr:callOI?putOI/callOI:null,callOIChange:callChange,putOIChange:putChange,netOIChange:putChange-callChange};}
function normalCdf(x){const sign=x<0?-1:1,a=Math.abs(x)/Math.sqrt(2),t=1/(1+0.3275911*a),y=1-((((1.061405429*t-1.453152027)*t+1.421413741)*t-0.284496736)*t+0.254829592)*t*Math.exp(-a*a);return .5*(1+sign*y);}
function greeksFromQuote({spot,strike,iv=0,timeYears=0,rate=.06,type='CE'}={}){if(![spot,strike,iv,timeYears].every(Number.isFinite)||spot<=0||strike<=0||iv<=0||timeYears<=0)return null;const d1=(Math.log(spot/strike)+(rate+iv*iv/2)*timeYears)/(iv*Math.sqrt(timeYears));const pdf=Math.exp(-d1*d1/2)/Math.sqrt(2*Math.PI);return {delta:type==='CE'?normalCdf(d1):normalCdf(d1)-1,gamma:pdf/(spot*iv*Math.sqrt(timeYears)),vega:spot*pdf*Math.sqrt(timeYears),d1};}
module.exports={optionChainMetrics,greeksFromQuote};
