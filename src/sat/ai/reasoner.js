function buildVerdict(input){const reasons=[];if(input.regime)reasons.push(`Regime: ${input.regime}`);if(input.evidence?.technical)reasons.push('Technical evidence present');if(input.evidence?.derivatives)reasons.push('Derivatives evidence present');if(input.evidence?.historical)reasons.push('Historical evidence present');const confidence=Number.isFinite(input.confidence)?Math.max(0,Math.min(1,input.confidence)):0;return {verdict:confidence>=.75?'HIGH_CONFIDENCE':confidence>=.55?'WATCH':'NO_TRADE',confidence,reasons,disclaimer:'Advisory output; human approval required before execution.'};}
async function openAIReason({apiKey,model='gpt-5-mini',input}){if(!apiKey)throw new Error('OPENAI_API_KEY_REQUIRED');const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({model,input})});if(!r.ok)throw new Error(`OPENAI_API_ERROR_${r.status}`);return r.json();}
module.exports={buildVerdict,openAIReason};
